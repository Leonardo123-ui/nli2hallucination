# 基础依赖
torch>=2.0.0
transformers>=4.30.0
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
tqdm>=4.64.0
requests>=2.28.0

# 可视化（可选）
matplotlib>=3.5.0
seaborn>=0.12.0

# API支持（可选，根据使用的API安装）
# 阿里云Qwen API
# dashscope>=1.0.0

# OpenAI API（支持llama和其他兼容API）
# openai>=1.0.0

# 注意：
# 1. torch的安装可能需要指定CUDA版本，例如：
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 2. 完整的推荐安装命令：
#    pip install torch transformers pandas numpy scikit-learn tqdm requests
#
# 3. 如果使用HuggingFace模型，需要HF_TOKEN：
#    huggingface-cli login
#
# 4. 使用Ollama时无需安装pytorch（Ollama自带）
